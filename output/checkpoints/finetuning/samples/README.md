# Estructura de `output/checkpoints/finetuning/samples`

Muestras de escucha generadas durante el entrenamiento de fine-tuning.

```text
output/checkpoints/finetuning/samples/
`-- <muestras por update de entrenamiento>
```

El contenido se genera automáticamente y no se versiona.
