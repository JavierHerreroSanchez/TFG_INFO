# Estructura de `output/checkpoints/finetuning/samples`

Esta carpeta almacena muestras de escucha generadas durante el entrenamiento de fine-tuning.

Estructura prevista:

```text
output/checkpoints/finetuning/samples/
└── <muestras por update de entrenamiento>
```

El contenido se excluye del repositorio porque se genera automaticamente durante los experimentos.
