# Estructura de `data/interim/tokenized_finetuning_v3`

Esta carpeta contiene la version principal de JSON tokenizados usada por `finetuning_v2.py`.

Estructura prevista:

```text
data/interim/tokenized_finetuning_v3/
└── <json tokenizados del corpus objetivo>
```

Los archivos internos se corresponden con piezas y variantes aumentadas del corpus de sonatas preparado para el segundo flujo experimental.
